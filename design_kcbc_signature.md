# KCBC 시그니처 디자인 시스템

이 저장소의 모든 화면은 아래 토큰·컴포넌트 규칙으로 통일한다. 새 스타일을 발명하지 않는다.

## 컬러 토큰

| 역할 | 기본 | 진하게 | 연하게 |
|---|---|---|---|
| 파랑 (표준·기본 액션) | `#2563eb` | `#1d4ed8` | `#dbeafe` |
| 보라 (독립·주요 생성) | `#7c3aed` | `#6d28d9` | `#ede9fe` |
| 초록 (지원·완료) | `#16a34a` | — | `#dcfce7` |
| 앰버 (경고·stale) | `#d97706` | — | `#fef3c7` |
| 빨강 (위험·즉시개입) | `#dc2626` | `#991b1b` | `#fee2e2` |

중립: 네이비 `#1e3a8a`(패널 헤더) · ink `#1e293b` · muted `#64748b` · line `#e2e8f0` · bg `#f1f5f9` · card `#fff`

주요 생성 버튼 그라디언트: `#8b5cf6 → #7c3aed`

## 타이포 · 접근성

- 폰트 스택: Pretendard → Malgun Gothic → system
- 본문 **18px**, 제목 **800 weight**, 라운드 **16px** (`--r`)
- 버튼 최소 높이 **44~56px**, 대비 **4.5:1**, **320px** 반응형, **A4 인쇄** 대응

## 컴포넌트

- **카드** — `#fff` + `1px solid #e2e8f0` + `border-radius:16px` + `0 1px 3px` 그림자. 헤더(`.card-h`)에 STEP pill 배지 + 상태칩
- **상태칩** `.state` — `ok`(초록) / `save`(파랑) / `stale`(앰버)
- **STEP pill** — 파랑, 완료 시 초록
- **세그먼트 토글** — `.seg` / `.seg.on`
- **주요 생성 버튼** — 보라 그라디언트 풀폭 `.generate`

## 레이아웃

- 좌 **380px** 입력/요약 + 우 미리보기 **2컬럼** (`.grid`), **960px 이하 1컬럼**
- 화면당 주요 동작 1개, 완료 단계는 요약칩으로 접기, 상태 항상 표시

## 이미지·자료(stimuli) 규칙

**카툰 삽화 금지.** 기능적 자료 카드(`.stim`)로 대체 — 비교표 · 지도 · 타임라인(inline SVG) · 인용문(원형 이니셜 아바타) · 다이어그램.

---

## 이 프로젝트에서의 적용 (학생맞춤통합지원 도구)

원 명세는 KCBC 평가 작성기 기준이라, 학맞통 도구에는 다음과 같이 대응시킨다.

### 6개 지원영역 색 — 명세 팔레트 안에서 구분 유지

지도 마커 · 카드 좌측 띠 · 범례 · 타임라인 막대가 이 색으로 서로 연결되므로 **구분을 없애지 않는다.**
단, 임의 색을 쓰지 않고 명세 팔레트로 재매핑한다.

| 지원영역 | 색 | 명세 대응 |
|---|---|---|
| 돌봄·안전 | `#dc2626` | 빨강 |
| 학업 | `#2563eb` | 파랑 |
| 건강 | `#16a34a` | 초록 |
| 심리·정서 | `#7c3aed` | 보라 |
| 경제 | `#d97706` | 앰버 |
| 기타 | `#64748b` | muted |

### 도구별 액센트

| 도구 | 색 |
|---|---|
| 진단 체크리스트 | 파랑 `#2563eb` |
| 지역자원 지도 | 초록 `#16a34a` |
| 지역자원 카드 | 앰버 `#d97706` |
| 의뢰서 작성기 | 파랑 진하게 `#1d4ed8` |
| 사례회의록 | 보라 `#7c3aed` |
| 지원 이력 대시보드 | 보라 진하게 `#6d28d9` |
| 안내 페이지 | 빨강 `#dc2626` |

### 유지하는 예외

- **즉시개입 · 중단 건 경고색** — 놓치면 안 되는 정보라 대비를 낮추지 않는다
- **의뢰서 · 회의록 A4 인쇄 서식** — 공문서 형식이므로 흑백 실선 유지 (`@media print`에서 색 제거)
